package screenControl;
import javafx.scene.Scene;

public class VoteScreen {

    public Scene draw(){
        // Code to draw the Vote Screen
        return new Scene(null);
    }
    //this will be the screen that the user will see when they are voting, it will have the propositions and the buttons to vote
    //when a button is pressed, it calls a display method to update the information, which then goes to the controller. 

    //THIS IS A TEMPLATE for all vote screens, the only thing that will change is the propositions that are displayed
    
}
